<% in_mobile = local_assigns.fetch(:in_mobile, browser.mobile?) %>

<%= render(Primer::Beta::Flash.new(my: 2, mx: in_mobile ? 0 : 5, scheme: :default)) do %>
  <div class="wiki op-uc-container">
    <%= format_text(Setting.welcome_text, headings: false) %>
  </div>
<% end %>
